export type Link = {
  label: string;
  href: string;
};

export type NavLink = Link;

export type HeroContent = {
  badge: string;
  eyebrow: string;
  title: string;
  description: string;
  ctaLabel: string;
  ctaHint: string;
};
